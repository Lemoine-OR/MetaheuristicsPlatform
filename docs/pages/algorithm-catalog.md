@page algorithm_catalog Algorithm catalog

# Algorithm catalog

The canonical machine-readable inventory is `docs/algorithm-catalog.json`.
The repository build validates that every catalog entry has a dedicated algorithm page,
stable ID, complexity, applicability, mathematical details and scientific provenance.

@page simple_api Common API

# Common API

Public algorithms use the common optimization lifecycle built around
`IMetaheuristic<TSolution,TParameters>`, `OptimizationContext<TSolution>`,
`IStoppingCriterion`, callbacks, deterministic random sources and
`OptimizationResult<TSolution>`.

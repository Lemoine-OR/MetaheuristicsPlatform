@page validation_benchmarks Validation and benchmarks

# Validation and benchmarks

BenchmarkDotNet suites are used to calibrate execution policies and quantify hot-path
cost. Deterministic tests validate sequential/parallel equivalence where the algorithm
semantics permit parallel execution.
